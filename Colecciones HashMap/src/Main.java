import javax.swing.*;
import java.util.HashMap;
import Agenda_Telefonos.Agenda_clase;

public class Main {
    public static void main(String[] args) {
        String Persona;
        int Telefono;
        int i = 0;
        HashMap<String, Integer> agenda = new HashMap<String, Integer>();
        while (i != 0){
            Persona = JOptionPane.showInputDialog("Ingrese el nombre de la Persona: ");
            Telefono = Integer.parseInt(JOptionPane.showInputDialog("Ingrese el Numero de Telefono: "));
            if (boolean containsKey(Object O);)agenda.put(Persona,  Telefono);

        }
    }
}